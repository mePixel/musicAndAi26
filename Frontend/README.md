# Bodybeat frontend

From this folder, run `npm install` and `npm run dev`. Use `npm test` for the
focused logic tests and `npm run build` for the static production build.

See the [root README](../README.md) for gameplay, camera setup, assets, and the
small source layout.
